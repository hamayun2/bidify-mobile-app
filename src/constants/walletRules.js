/** Minimum wallet balance (PKR) required before placing any auction bid. */
export const MIN_WALLET_BALANCE_TO_BID_PKR = 1000;

/** Preset top-up amounts shown on Add Funds UI (PKR). */
export const WALLET_TOPUP_PRESETS_PKR = [5000, 10000, 15000];
